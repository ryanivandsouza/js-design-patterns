import React from 'react'

export default ({ children }) =>
  <div
    style={{
        fontSize: '14px',
        textAlign: 'left',
        width: '30em',
        alignItems: 'top'
    }}>
    {children}
  </div>
  